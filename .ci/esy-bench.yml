# Run benchmarks

steps:
  - script: esy @bench install
    displayName: 'esy @bench install'
  - script: esy @bench build
    displayName: 'esy @bench build'
  - script: esy @bench x revery-bench
    displayName: 'esy @bench x revery-bench'
